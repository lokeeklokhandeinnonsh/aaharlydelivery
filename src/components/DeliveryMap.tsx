import React from 'react';
import { View, Text } from 'react-native';

const DeliveryMap = (props: any) => (
    <View style={{ flex: 1, backgroundColor: '#1C1917', justifyContent: 'center', alignItems: 'center', borderRadius: 16 }}>
        <Text style={{ color: '#A8A29E', fontSize: 12 }}>Internal Map Disabled for Stability</Text>
    </View>
);

export default DeliveryMap;
